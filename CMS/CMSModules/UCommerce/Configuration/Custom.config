<!--
	Custom configuration for uCommerce components.
	
	Find the ID of component you want to override
	and use the same ID here. Your component will
	override the default.
	
	You can configure new components here as well
	and have them injected into existing ones.
-->
<configuration>
	<components>
		<component id="SearchSessionProvider" service="UCommerce.Search.RavenDB.IRavenDbStoreProvider, UCommerce" type="UCommerce.Search.RavenDB.RavenDbIisStoreProvider, UCommerce">
		<parameters>
        <DatabaseName>KenticoWF</DatabaseName>
        <Url>http://ravendb</Url>
    </parameters>
</component>
	</components>
</configuration>
	